# Azeta Homes does not require any environment variables to build or run.
# This file is kept as a placeholder so the .gitignore rule (!.env.example) still
# matches something. Add VITE_-prefixed variables here if frontend env vars become
# necessary, and document each one inline.
